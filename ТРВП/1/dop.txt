// пишем go-приложение

// собираем его через WSL (так будет удобнее, чем писать всякие переменные окружения)
go build -o server-go

// создаем dockerfile для создания образа нашего приложения(прилагается со всем остальным добром)

// создаем конфиг для nginx(по сути стандартный конфиг; тоже прилагается)

// создаем конфиг для squid(ну тут нужно было посмотреть, погуглить поискать, т.к. новенькое, не проходили, не пробовали; тоже прилагается)

// создаем файлик docker-compose.yml(упрощаем себе жизнь; тоже прилагается)

// запускаем наши контейнеры(или же без -d, чтобы не в фоне (это на следующий раз docker compose up))
docker compose up --build -d

// просмотр логов
docker compose logs -f

// остановка контейнеров
docker compose down

// запрос через nginx(можно показать например через postman)
http://localhost:8080/ 

// запрос, чтобы показать еще и Squid
curl -x http://localhost:3128 http://nginx/

-x http://localhost:3128 — использовать Squid как прокси
http://nginx/ — целевой сервер, то есть Nginx внутри Docker-сети

curl → Squid → Nginx → back-go


///// это при запросе напрямую к nginx
// это в нашем случае NGINX
RemoteAddr: 172.19.0.3:54760

// а это типо информация о реальном клиенте
X-Real-IP: 172.19.0.1         
X-Forwarded-For: 172.19.0.1

// а это, что запрос был отправлен с POSTMAN
PostmanRuntime/7.51.1

///// а это еще с Squid
Host: nginx
RemoteAddr: 172.19.0.3:45510
X-Real-IP: 172.19.0.4
X-Forwarded-For: 172.19.0.1, 172.19.0.4

// это в нашем случае NGINX
RemoteAddr: 172.19.0.3

// это Squid
X-Real-IP: 172.19.0.4

// а тут цепочка, где показано, что сначала клинет, потом Squid
X-Forwarded-For: 172.19.0.1, 172.19.0.4
